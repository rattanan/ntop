import {describe,expect,it}from"vitest";import{calculateProspectScore,canTransitionProspect,normalizeProspectText,normalizeWebsiteDomain,selectProspectLeadPhone}from"../../lib/prospect/prospect-rules";describe("Prospect rules",()=>{it("allows direct movement between active statuses while preserving terminal command boundaries",()=>{expect(canTransitionProspect("NEW","QUALIFIED")).toBe(true);expect(canTransitionProspect("LOST","CONTACTED")).toBe(true);expect(canTransitionProspect("QUALIFIED","NEW")).toBe(true);expect(canTransitionProspect("NEW","ARCHIVED")).toBe(false);expect(canTransitionProspect("NEW","CONVERTED")).toBe(false);expect(canTransitionProspect("CONVERTED","NEW")).toBe(false);expect(canTransitionProspect("ARCHIVED","NEW")).toBe(false);});it("normalizes duplicate signals",()=>{expect(normalizeProspectText("บริษัท Acme จำกัด")).toBe("acme");expect(normalizeWebsiteDomain("https://www.example.com/path")).toBe("example.com");});it("uses the Prospect phone when mobile is blank during Lead conversion",()=>{expect(selectProspectLeadPhone({mobile:"",phone:"081 855 8081"})).toBe("081 855 8081");expect(selectProspectLeadPhone({mobile:"089 000 0000",phone:"02 123 4567"})).toBe("089 000 0000");expect(selectProspectLeadPhone({mobile:" ",phone:" 02 123 4567 "})).toBe("02 123 4567");});it("calculates bounded heat score independently from UI",()=>{const result=calculateProspectScore({status:"QUALIFIED",source:"REFERRAL",estimatedOpportunityValue:"1000",numberOfEmployees:500,numberOfBranches:5,industryId:"industry",expectedBudget:"900",expectedPurchasePeriod:"Q4",contactAttemptCount:2,aiOpportunityScore:90,lastContactAt:new Date("2026-07-10T00:00:00Z"),completeness:100},new Date("2026-07-14T00:00:00Z"));expect(result.score).toBeGreaterThanOrEqual(75);expect(result.heatLevel).toBe("HOT");expect(result.score).toBeLessThanOrEqual(100);});});
