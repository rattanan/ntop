import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";const read=(path:string)=>readFileSync(path,"utf8");
describe("Contract REST and migration contract",()=>{it("requires authentication and idempotency for mutation routes",()=>{for(const route of ["app/api/v1/contracts/route.ts","app/api/v1/contracts/[id]/route.ts","app/api/v1/contracts/[id]/status/route.ts","app/api/v1/contracts/[id]/documents/route.ts","app/api/v1/contracts/[id]/signatures/route.ts","app/api/v1/contracts/[id]/amendments/route.ts","app/api/v1/contracts/[id]/renewals/route.ts","app/api/v1/contracts/[id]/purchase-orders/route.ts","app/api/v1/contracts/[id]/service-orders/route.ts"]){const source=read(route);expect(source).toContain("getSession");expect(source).toContain("requireIdempotencyKey");}});it("keeps migration expand-only and uses Decimal financial columns",()=>{const migration=read("prisma/migrations/20260715213000_add_contract_management/migration.sql");expect(migration).toMatch(/CREATE TABLE (?:IF NOT EXISTS )?`ContractVersion`/);expect(migration).toContain("DECIMAL(19, 4)");expect(migration).not.toMatch(/DROP TABLE|DROP COLUMN/);});it("keeps AI provider configurable and analysis state-free",()=>{const route=read("app/api/v1/contracts/[id]/ai-analysis/route.ts");expect(route).toContain("createActiveProviderClient");expect(route).not.toContain('model: "');expect(route).not.toMatch(/contract\.(update|create)/);});});
