import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const read=(p:string)=>readFileSync(p,"utf8");
describe("Presales REST and security contract",()=>{
  it("authenticates every API through a scoped actor helper",()=>{const helper=read("app/api/v1/presales-api.ts");expect(helper).toContain("getSession");expect(helper).toContain("loadAuthorizationContext");for(const route of ["app/api/v1/solution-designs/route.ts","app/api/v1/site-surveys/route.ts","app/api/v1/boqs/route.ts","app/api/v1/site-surveys/[id]/results/review/route.ts"])expect(read(route)).toContain("presalesActor");});
  it("routes mutations through server domain services rather than Prisma in UI/routes",()=>{for(const route of ["app/api/v1/solution-designs/[id]/services/route.ts","app/api/v1/solution-designs/[id]/sites/route.ts","app/api/v1/site-surveys/[id]/submit/route.ts","app/api/v1/site-surveys/[id]/create-boq-draft/route.ts"]){const source=read(route);expect(source).not.toContain("prisma.");expect(source).toMatch(/addSolutionService|addInstallationSite|submitSiteSurveyRequest|createBoqFromSurvey/);}});
  it("does not contain a production NTSP network adapter",()=>{const provider=read("lib/solution-design/site-survey-provider.ts");expect(provider).not.toContain("fetch(");expect(provider).not.toContain("axios");expect(provider).toContain('environment === "production"');});
  it("enforces assigned-only survey execution and maker-checker review",()=>{const service=read("lib/solution-design/solution-design-service.ts");expect(service).toContain("request.assignedSurveyEngineerId!==actor.id");expect(service).toContain("result.submittedById===actor.id");expect(service).toContain("buildOpportunityScopeWhere");});
});
