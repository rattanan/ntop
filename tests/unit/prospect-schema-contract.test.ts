import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const schema=readFileSync("prisma/schema.prisma","utf8"),migration=readFileSync("prisma/migrations/20260714220000_add_prospect_management/migration.sql","utf8"),service=readFileSync("lib/prospect/prospect-service.ts","utf8");describe("Prospect schema and conversion contract",()=>{it("uses additive soft-delete, decimal money and idempotency models",()=>{expect(schema).toContain("model Prospect");expect(schema).toContain("deletedAt");expect(schema).toContain("@db.Decimal(19, 4)");expect(schema).toContain("model ProspectCommandReceipt");expect(migration).not.toMatch(/DROP\s+(TABLE|COLUMN)/i);});it("links contacts, activities and documents in the conversion transaction",()=>{expect(service).toContain("prospectContact.updateMany");expect(service).toContain("activity.updateMany");expect(service).toContain("salesDocument.updateMany");expect(service).toContain('action: "prospect.convert"');expect(service).toContain("convertedLeadId: null");});});
