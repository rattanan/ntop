import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
const route=readFileSync("app/api/v1/leads/route.ts","utf8");const importRoute=readFileSync("app/api/v1/leads/import/route.ts","utf8");const previewRoute=readFileSync("app/api/v1/leads/import/preview/route.ts","utf8");const mergeRoute=readFileSync("app/api/v1/leads/[id]/merge/route.ts","utf8");const viewsRoute=readFileSync("app/api/v1/leads/views/route.ts","utf8");const rulesRoute=readFileSync("app/api/v1/leads/assignment-rules/route.ts","utf8");
describe("Lead API contract",()=>{it("enforces session, scoped list and idempotent create",()=>{expect(route).toContain("getSession");expect(route).toContain("buildLeadScopeWhere");expect(route).toContain("requireIdempotencyKey");expect(route).toContain("createLeadRuntime");});it("restricts import roles, size and row count and previews scoped duplicates",()=>{expect(importRoute).toContain("LEAD_IMPORT_ROLES");expect(importRoute).toContain("2_000_000");expect(importRoute).toContain("records.length>1000");expect(importRoute).toContain("createLeadRuntime().create");expect(previewRoute).toContain("buildLeadScopeWhere");expect(previewRoute).toContain("duplicateCandidates");});it("keeps saved views user-scoped and assignment rules admin-scoped",()=>{expect(viewsRoute).toContain("userId: session.id");expect(viewsRoute).toContain("$transaction");expect(rulesRoute).toContain("LEAD_ASSIGNMENT_RULE_ADMIN_ROLES");expect(rulesRoute).toContain("createLeadAuditWriter");expect(rulesRoute).toContain("$transaction");});it("merges activities in an audited idempotent transaction",()=>{expect(mergeRoute).toContain("leadCommandReceipt.findUnique");expect(mergeRoute).toContain("activity.updateMany");expect(mergeRoute).toContain('action:"lead.merge"');expect(mergeRoute).toContain("mergedIntoLeadId");});});
