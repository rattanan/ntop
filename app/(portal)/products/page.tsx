import Link from "next/link";
import { prisma } from "@/lib/prisma";
import { requireSession } from "@/lib/auth";
export default async function Products(){const s=await requireSession();const products=await prisma.product.findMany({orderBy:[{category:"asc"},{name:"asc"}]});return <><div className="page-head"><div><p className="eyebrow">Product Catalog</p><h1>บริการและโซลูชัน NT</h1></div>{s.role==="ADMIN"&&<Link href="/products/new" className="primary">เพิ่มบริการ</Link>}</div><section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>รหัส</th><th>บริการ</th><th>หมวดหมู่</th><th>ราคาอ้างอิง</th><th>สถานะ</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.code}</td><td><strong>{p.name}</strong><br/><small>{p.description||"—"}</small></td><td><span className="badge">{p.category}</span></td><td>{new Intl.NumberFormat("th-TH",{style:"currency",currency:"THB",maximumFractionDigits:0}).format(Number(p.listPrice))}</td><td>{p.active?"ใช้งาน":"ปิดใช้งาน"}</td></tr>)}</tbody></table>{!products.length&&<div className="empty">ยังไม่มีบริการใน Catalog — ผู้ดูแลระบบสามารถเพิ่ม Internet, MPLS, SD-WAN, Cloud และบริการอื่นได้</div>}</div></section></>}
