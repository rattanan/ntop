import{notFound}from"next/navigation";import{ResultActions,SurveyCommandForms,SurveyResultForm}from"@/components/presales-forms";import{requireSession}from"@/lib/auth";import{loadAuthorizationContext}from"@/lib/authorization/authorization-context";import{prisma}from"@/lib/prisma";import{getSiteSurvey,PresalesAccessError}from"@/lib/solution-design/solution-design-service";
export default async function SiteSurveyDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const session=await requireSession();const authorization=await loadAuthorizationContext({actorId:session.id,legacyRole:session.role});let survey;try{survey=await getSiteSurvey({...session,authorization},id);}catch(error){if(error instanceof PresalesAccessError)notFound();throw error;}const[users,organizationUnits]=await Promise.all([prisma.user.findMany({where:{active:true},select:{id:true,name:true},orderBy:{name:"asc"}}),prisma.organizationUnit.findMany({where:{active:true},select:{id:true,name:true,code:true},orderBy:{name:"asc"}})]);return <><div className="page-head"><div><p className="eyebrow">{survey.surveyRequestNumber}</p><h1>{survey.site?.siteName??"Site Survey"}</h1><p>Integration Mode: {survey.integrationMode} · Provider: {survey.integrationProvider}</p></div><span className="badge">{survey.statusCode}</span></div><section className="presales-kpis"><article className="card"><span>Priority</span><strong>{survey.priority}</strong></article><article className="card"><span>Scheduled</span><strong>{survey.scheduledSurveyDate?.toLocaleDateString("th-TH")??"—"}</strong></article><article className="card"><span>Result revisions</span><strong>{survey.results.length}</strong></article></section><section className="card"><div className="card-header"><strong>Installation Site &amp; GPS</strong></div><div className="card-body detail-grid"><div><span>Address</span><strong>{survey.site?.addressLine1}, {survey.site?.district}, {survey.site?.province}</strong></div><div><span>Coordinates</span><strong>{survey.site?.latitude.toString()}, {survey.site?.longitude.toString()}</strong></div><div><span>Contacts</span><strong>{survey.contacts.map(c=>`${c.fullName} · ${c.phone}`).join(", ")}</strong></div></div></section><SurveyCommandForms requestId={id} status={survey.statusCode} users={users} organizationUnits={organizationUnits}/>{survey.statusCode==="IN_PROGRESS"&&<SurveyResultForm requestId={id}/>}<ResultActions requestId={id} status={survey.statusCode}/><section className="card"><div className="card-header"><strong>Survey Results</strong></div><div className="card-body related-list">{survey.results.map(r=><article key={r.id}><strong>Revision {r.revisionNumber} · {r.feasibilityStatus}</strong><p>{r.technicalSummary}</p><small>{r.statusCode} · Source {r.resultSource}</small></article>)}</div></section><section className="card"><div className="card-header"><strong>Estimated Items</strong></div><div className="table-wrap"><table className="table"><thead><tr><th>Type</th><th>Item</th><th>Qty</th><th>Estimated cost</th></tr></thead><tbody>{survey.estimatedItems.map(i=><tr key={i.id}><td>{i.itemType}</td><td>{i.itemName}</td><td>{i.quantity.toString()} {i.unit}</td><td>{i.estimatedTotalCost?.toString()??"—"}</td></tr>)}</tbody></table></div></section><section className="card"><div className="card-header"><strong>Integration History</strong></div><div className="card-body timeline-list">{survey.logs.map(log=><div className="timeline" key={log.id}><strong>{log.provider} · {log.status}</strong><p>Schema {log.schemaVersion} · {log.direction}</p><small>{log.createdAt.toLocaleString("th-TH")}</small></div>)}</div></section></>}
