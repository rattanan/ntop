import Link from "next/link";import {notFound}from"next/navigation";import{AddComponentForm,AddRiskForm,AddServiceForm,AddSiteForm,CreateSurveyForm,SolutionWorkflowForm}from"@/components/presales-forms";import{requireSession}from"@/lib/auth";import{loadAuthorizationContext}from"@/lib/authorization/authorization-context";import{prisma}from"@/lib/prisma";import{getSolutionDesign,PresalesAccessError}from"@/lib/solution-design/solution-design-service";
export default async function SolutionDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const session=await requireSession();const authorization=await loadAuthorizationContext({actorId:session.id,legacyRole:session.role});let design;try{design=await getSolutionDesign({...session,authorization},id);}catch(error){if(error instanceof PresalesAccessError)notFound();throw error;}const[categories,products]=await Promise.all([prisma.serviceCategoryConfig.findMany({where:{active:true},orderBy:{displayOrder:"asc"}}),prisma.product.findMany({where:{active:true},select:{id:true,name:true,code:true},orderBy:{name:"asc"}})]);return <><div className="page-head"><div><p className="eyebrow">{design.solutionDesignNumber}</p><h1>{design.solutionDesignName}</h1><p>Opportunity <Link href={`/opportunities/${design.opportunityId}`}>เปิดรายการ</Link> · Version {design.version}.{design.revisionNumber}</p></div><span className="badge">{design.statusCode}</span></div><section className="presales-kpis"><article className="card"><span>Overall readiness</span><strong>{design.overallReadiness}%</strong></article><article className="card"><span>Technical feasibility</span><strong>{design.technicalFeasibility}</strong></article><article className="card"><span>Survey</span><strong>{design.surveyRequired?"Required":"Not required"}</strong></article></section><SolutionWorkflowForm designId={id} status={design.statusCode}/><nav className="opportunity-tabs"><a href="#services">Services</a><a href="#sites">Sites</a><a href="#components">Components</a><a href="#surveys">Surveys</a><a href="#boqs">BOQ</a><a href="#traceability">Traceability</a><a href="#versions">Versions</a></nav><section className="card" id="services"><div className="card-header"><strong>Products &amp; Services</strong><span>{design.services.length}</span></div><div className="card-body related-list">{design.services.map((s,i)=><article key={s.id}><strong>Service {i+1} · {s.requestedBandwidth??"ไม่ระบุ bandwidth"}</strong><p>{s.accessTechnology??"Any access technology"}</p><small>{s.surveyRequired?"Survey required":"No survey"} · {s.boqRequired?"BOQ required":"No BOQ"}</small></article>)}</div></section><AddServiceForm designId={id} categories={categories} products={products}/><section className="card" id="sites"><div className="card-header"><strong>Installation Sites</strong><span>{design.sites.length}</span></div><div className="card-body related-list">{design.sites.map(s=><article key={s.id}><strong>{s.siteCode?`${s.siteCode} · `:""}{s.siteName}</strong><p>{s.addressLine1}, {s.district}, {s.province}</p><small>{s.latitude.toString()}, {s.longitude.toString()}</small></article>)}</div></section><AddSiteForm designId={id}/><section className="card" id="components"><div className="card-header"><strong>Solution Components &amp; Network</strong><span>{design.components.length} components · {design.networkConnections.length} links</span></div><div className="card-body related-list">{design.components.map(c=><article key={c.id}><strong>{c.componentNumber} · {c.componentName}</strong><p>{c.componentType} · {c.bandwidth??"—"}</p></article>)}</div></section><AddComponentForm designId={id} sites={design.sites}/>{design.services.length>0&&design.sites.length>0&&<CreateSurveyForm designId={id} sites={design.sites} services={design.services}/>}<section className="card" id="surveys"><div className="card-header"><strong>Site Surveys</strong><span>{design.surveys.length}</span></div><div className="card-body related-list">{design.surveys.map(s=><article key={s.id}><Link className="link" href={`/site-surveys/${s.id}`}>{s.surveyRequestNumber}</Link><p>{s.statusCode} · Integration Mode: {s.integrationMode}</p></article>)}</div></section><section className="card" id="boqs"><div className="card-header"><strong>BOQ</strong><span>{design.boqs.length}</span></div><div className="card-body related-list">{design.boqs.map(b=><article key={b.id}><Link className="link" href={`/boqs/${b.id}`}>{b.boqNumber}</Link><p>{b.statusCode} · {b.currency} {b.totalContractValue.toString()}</p></article>)}</div></section><section className="card" id="traceability"><div className="card-header"><strong>Requirement Traceability &amp; Risks</strong></div><div className="card-body"><p>{design.mappings.length} mappings · {design.risks.length} assumptions/constraints/risks</p></div></section><AddRiskForm designId={id}/><section className="card" id="versions"><div className="card-header"><strong>Version &amp; Review History</strong></div><div className="card-body timeline-list">{design.versions.map(v=><div className="timeline" key={v.id}><strong>Version {v.version}.{v.revisionNumber}</strong><p>{v.statusCode}</p><small>{v.createdAt.toLocaleString("th-TH")}</small></div>)}{design.reviews.map(v=><div className="timeline" key={v.id}><strong>{v.reviewType} · {v.decision}</strong><p>{v.reason}</p><small>{v.createdAt.toLocaleString("th-TH")}</small></div>)}</div></section></>}
