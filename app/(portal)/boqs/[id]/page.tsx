import{notFound}from"next/navigation";import{BoqCommandForms}from"@/components/presales-forms";import{requireSession}from"@/lib/auth";import{loadAuthorizationContext}from"@/lib/authorization/authorization-context";import{prisma}from"@/lib/prisma";import{getBoq,PresalesAccessError}from"@/lib/solution-design/solution-design-service";
export default async function BoqDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const session=await requireSession();const authorization=await loadAuthorizationContext({actorId:session.id,legacyRole:session.role});let boq;try{boq=await getBoq({...session,authorization},id);}catch(error){if(error instanceof PresalesAccessError)notFound();throw error;}const sections=await prisma.boqSection.findMany({where:{active:true},select:{id:true,name:true},orderBy:{displayOrder:"asc"}});return <><div className="page-head"><div><p className="eyebrow">{boq.boqNumber}</p><h1>BOQ Version {boq.version}.{boq.revisionNumber}</h1><p>Pricing date {boq.pricingDate.toLocaleDateString("th-TH")}</p></div><span className="badge">{boq.statusCode}</span></div><section className="presales-kpis"><article className="card"><span>Total contract value</span><strong>{boq.currency} {boq.totalContractValue.toString()}</strong></article><article className="card"><span>Total cost</span><strong>{boq.totalCost?.toString()??"Restricted"}</strong></article><article className="card"><span>Gross margin</span><strong>{boq.grossMarginPercent?.toString()??"Restricted"}%</strong></article></section><section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>Line</th><th>Item</th><th>Source</th><th>Qty</th><th>Charge</th><th>Cost</th><th>Selling</th></tr></thead><tbody>{boq.items.map(i=><tr key={i.id}><td>{i.lineNumber}</td><td>{i.itemName}<br/><small>{i.provisionalPricing?"Provisional pricing":"Confirmed"}</small></td><td>{i.sourceType}</td><td>{i.finalQuantity.toString()} {i.unit}</td><td>{i.chargeType}</td><td>{i.totalCost?.toString()??"Restricted"}</td><td>{i.totalSellingPrice.toString()}</td></tr>)}</tbody></table></div></section><BoqCommandForms boqId={id} status={boq.statusCode} sections={sections}/><section className="card"><div className="card-header"><strong>Version History</strong></div><div className="card-body timeline-list">{boq.versions.map(v=><div className="timeline" key={v.id}><strong>{v.version}.{v.revisionNumber} · {v.statusCode}</strong><p>{v.changeReason??"Initial snapshot"}</p><small>{v.createdAt.toLocaleString("th-TH")}</small></div>)}</div></section></>}
