import Link from"next/link";import{requireSession}from"@/lib/auth";import{loadAuthorizationContext}from"@/lib/authorization/authorization-context";import{listBoqs}from"@/lib/solution-design/solution-design-service";
export default async function BoqsPage(){const session=await requireSession();const authorization=await loadAuthorizationContext({actorId:session.id,legacyRole:session.role});const boqs=await listBoqs({...session,authorization});return <><div className="page-head"><div><p className="eyebrow">Pre-Sales commercial</p><h1>Bill of Quantities</h1><p>One-time and recurring charges with version history</p></div></div><section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>BOQ</th><th>Status</th><th>Version</th><th>Contract value</th><th>Margin</th></tr></thead><tbody>{boqs.map(b=><tr key={b.id}><td><Link className="link" href={`/boqs/${b.id}`}>{b.boqNumber}</Link></td><td><span className="badge">{b.statusCode}</span></td><td>{b.version}.{b.revisionNumber}</td><td>{b.currency} {b.totalContractValue.toString()}</td><td>{b.grossMarginPercent?.toString()??"Restricted"}</td></tr>)}</tbody></table></div></section></>}
