import { OpportunityForm } from "@/components/forms";
import { prisma } from "@/lib/prisma";
import { requireSession } from "@/lib/auth";
import { buildAuthorizedUserWhere, loadAuthorizationContext } from "@/lib/authorization/authorization-context";
import { buildCustomerScopeWhere } from "@/lib/customer/customer-query-service";
export default async function NewOpportunityPage() { const session=await requireSession(); const authorization=await loadAuthorizationContext({actorId:session.id,legacyRole:session.role}); const [customers,users]=await Promise.all([prisma.customer.findMany({where:buildCustomerScopeWhere(authorization),select:{id:true,name:true,taxId:true},orderBy:{name:"asc"}}),session.role==="ADMIN"?prisma.user.findMany({where:buildAuthorizedUserWhere(authorization),select:{id:true,name:true,email:true},orderBy:{name:"asc"}}):Promise.resolve([])]); return <><div className="page-head"><div><p className="eyebrow">Pipeline</p><h1>สร้างโอกาสขายใหม่</h1></div></div>{customers.length?<OpportunityForm customers={customers} users={users} role={session.role}/>:<section className="card"><div className="empty">ยังไม่มี Customerในหน่วยงานที่คุณดูแล กรุณาสร้าง Customerก่อน</div></section>}</>; }
