import { NextResponse } from "next/server";import { getSession } from "@/lib/auth";import { workflowCorrelationId,workflowUnauthenticated } from "../../../workflow-api-response";
export async function GET(request:Request){const correlationId=workflowCorrelationId(request);if(!await getSession())return workflowUnauthenticated(correlationId);const content="company,contactName,contactEmail,contactPhone,source,recommendedProducts,requirementSummary,duplicateOverrideReason\r\n";return new NextResponse(content,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":"attachment; filename=lead-import-template.csv","x-correlation-id":correlationId}});}
