import { NextResponse } from "next/server";import { prospectImportTemplate } from "@/lib/prospect/prospect-import";import { prospectActor } from "../../prospect-api";export async function GET(request:Request){const auth=await prospectActor(request);if("response"in auth)return auth.response;return new NextResponse(prospectImportTemplate(),{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":"attachment; filename=prospect-import-template.csv"}});}
