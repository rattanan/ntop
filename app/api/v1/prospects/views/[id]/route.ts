import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { prospectActor,prospectApiError } from "../../prospect-api";export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await prospectActor(request);if("response"in auth)return auth.response;try{const {id}=await params,result=await prisma.prospectSavedView.deleteMany({where:{id,userId:auth.actor.id}});if(!result.count)return NextResponse.json({error:{code:"RESOURCE_NOT_FOUND",message:"ไม่พบ Saved Filter",retryable:false,correlationId:auth.correlationId}},{status:404});return NextResponse.json({data:{id},meta:{correlationId:auth.correlationId}});}catch(error){return prospectApiError(error,auth.correlationId);}}
