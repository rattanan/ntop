import { NextResponse } from "next/server";import { getSession } from "@/lib/auth";import { contractTransitionSchema } from "@/lib/contract/contracts";import { createContractRuntime } from "@/lib/contract/contract-runtime";import { requireIdempotencyKey,workflowApiError,workflowCorrelationId,workflowUnauthenticated } from "../../../workflow-api-response";import { contractActor } from "../../contract-api";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const correlationId=workflowCorrelationId(request),session=await getSession();if(!session)return workflowUnauthenticated(correlationId);const key=requireIdempotencyKey(request,correlationId);if(typeof key!=="string")return key;try{const input=contractTransitionSchema.parse(await request.json());const data=await createContractRuntime().service.transition(await contractActor(session),(await params).id,input.expectedVersion,input.toStatusCode,input.comment,correlationId);return NextResponse.json({data,meta:{correlationId}});}catch(error){return workflowApiError(error,correlationId);}}
