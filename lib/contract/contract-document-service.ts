import { createHash } from "node:crypto";import { z } from "zod";import type { AuditWriter } from "../audit/audit-writer";import type { AuthorizationContext } from "../authorization/authorization-context";import type { ProspectDocumentStorage,StoredDocument } from "../prospect/prospect-document-storage";import type { PrismaContractRepository } from "./prisma-contract-repository";import { ContractAccessError } from "./contract-service";import type { Prisma } from "@prisma/client";
const metadata=z.strictObject({category:z.string().trim().regex(/^[A-Z][A-Z0-9_]{1,59}$/),fileName:z.string().trim().min(1).max(255),mimeType:z.string().trim().min(1).max(191)}),allowed=new Set(["application/pdf","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document","application/vnd.ms-excel","application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","image/jpeg","image/png","application/zip","application/x-zip-compressed"]);export const MAX_CONTRACT_DOCUMENT_BYTES=25_000_000;
export class ContractDocumentValidationError extends Error{constructor(message:string){super(message);this.name="ContractDocumentValidationError";}}
export class ContractDocumentService{constructor(private repository:PrismaContractRepository,private audit:AuditWriter<Prisma.TransactionClient>,private storage:ProspectDocumentStorage){}async upload(actor:{id:string;authorization:AuthorizationContext},contractId:string,input:{category:string;fileName:string;mimeType:string;bytes:Uint8Array},correlationId:string,idempotencyKey:string){const parsed=metadata.safeParse(input);if(!parsed.success||!allowed.has(input.mimeType)||!input.bytes.length||input.bytes.length>MAX_CONTRACT_DOCUMENT_BYTES)throw new ContractDocumentValidationError("Contract document must be a supported PDF, Office, image or ZIP file up to 25 MB.");const safe=input.fileName.replace(/[\\/\u0000-\u001f]/g,"_"),sha256=createHash("sha256").update(input.bytes).digest("hex"),objectKey=`contracts/${contractId}/${input.category}/${sha256}/${safe}`,objectKeyHash=createHash("sha256").update(objectKey).digest("hex");const replay=await this.repository.transaction(async tx=>{const contract=await this.repository.find(contractId,actor.authorization,tx);if(!contract)throw new ContractAccessError();return tx.contractDocumentVersion.findUnique({where:{objectKeyHash}});});if(replay)return replay;const stored:StoredDocument={objectKey,contentHash:sha256,fileName:safe,mimeType:input.mimeType,sizeBytes:input.bytes.length};await this.storage.put(stored,input.bytes);try{await this.storage.assertClean(stored);return await this.repository.transaction(async tx=>{const contract=await this.repository.find(contractId,actor.authorization,tx);if(!contract)throw new ContractAccessError();const document=await tx.contractDocument.findFirst({where:{contractId,category:input.category}})??await tx.contractDocument.create({data:{contractId,category:input.category}});const versionNumber=document.currentVersion+1;const version=await tx.contractDocumentVersion.create({data:{documentId:document.id,versionNumber,fileName:safe,mimeType:input.mimeType,sizeBytes:input.bytes.length,objectKey,objectKeyHash,sha256,malwareScanStatus:"CLEAN",uploadedById:actor.id}});await tx.contractDocument.update({where:{id:document.id},data:{currentVersion:versionNumber}});await tx.contractCommandReceipt.create({data:{actorId:actor.id,idempotencyKey,command:"contract.document.upload",contractId,resultVersion:contract.version}});await this.audit.append({actorId:actor.id,action:"contract.document.upload",targetType:"ContractDocumentVersion",targetId:version.id,targetVersion:String(versionNumber),outcome:"SUCCESS",correlationId,data:{contractId,category:input.category,sha256,mimeType:input.mimeType,sizeBytes:input.bytes.length}},{transaction:tx});return version;});}catch(error){await this.storage.remove(objectKey);throw error;}}}
